# =============================================================================
# InfoHub Telegram Bot - Dependencies
# Оптимизировано для Render Free Tier (минимальный размер, максимальная скорость)
# =============================================================================

# -----------------------------------------------------------------------------
# ОСНОВНЫЕ ЗАВИСИМОСТИ (обязательные)
# -----------------------------------------------------------------------------

# Telegram Bot Framework (асинхронный)
# Версия 3.x имеет лучшую производительность и поддержку Python 3.10+
aiogram>=3.0,<4.0

# Async HTTP Client для запросов к внешним API
# Критично для кэширования данных (погода, крипта, новости)
aiohttp>=3.8,<4.0

# Планировщик задач для ежедневных рассылок
# Позволяет запускать задачи по расписанию без внешних cron-сервисов
apscheduler>=3.10,<4.0

# Асинхронная работа с SQLite
# Встроен в Python, но aiosqlite даёт async/await интерфейс
aiosqlite>=0.19,<1.0

# Загрузка переменных окружения из .env (для локальной разработки)
# На Render переменные задаются через Dashboard, но полезно для тестов
python-dotenv>=1.0,<2.0

# -----------------------------------------------------------------------------
# ОПЦИОНАЛЬНЫЕ ЗАВИСИМОСТИ (для будущего масштабирования)
# Раскомментируйте при переходе на production-уровень
# -----------------------------------------------------------------------------

# Для замены SQLite на PostgreSQL (когда пользователей > 1000)
# asyncpg>=0.28,<1.0

# Для замены JSON-кэша на Redis (когда нужно общее кэширование между инстансами)
# redis>=4.5,<5.0

# Для продвинутого логирования (красивые логи с цветами)
# loguru>=0.7,<1.0

# Для валидации настроек через Pydantic
# pydantic>=2.0,<3.0
# pydantic-settings>=2.0,<3.0

# Для health check endpoint (требуется некоторым хостингам)
# fastapi>=0.100,<1.0
# uvicorn>=0.22,<1.0

# -----------------------------------------------------------------------------
# ЗАВИСИМОСТИ ДЛЯ РАЗРАБОТКИ (не устанавливаются на production)
# Установите командой: pip install -r requirements-dev.txt
# -----------------------------------------------------------------------------

# pytest>=7.0,<8.0
# pytest-asyncio>=0.21,<1.0
# black>=23.0,<24.0
# flake8>=6.0,<7.0
# mypy>=1.0,<2.0

# -----------------------------------------------------------------------------
# ПРИМЕЧАНИЯ ДЛЯ RENDER FREE TIER
# -----------------------------------------------------------------------------
# - Все версии указаны с диапазоном (>=X,<Y) для гибкости установки
# - Render сам подберёт совместимые версии при сборке образа
# - Избегайте слишком строгих версий (==3.0.0) — это может вызвать конфликты
# - Общий размер зависимостей: ~15-20MB (вписывается в лимиты Free Tier)
# - Время установки: ~30-60 секунд при холодной сборке